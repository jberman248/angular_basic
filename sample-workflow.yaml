version: v1
description: Testing build speed with basic angular

steps:
  - name: Step-1-Install
    description: ""
    image: projectnebula/core:latest
    input:
      - npm install &&
        npm start

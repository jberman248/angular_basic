version: v1
description: Testing build speed with basic angular

steps:
  - name: Step-0-Install
    description: ""
    image: luther007/cynerge_images:latest
    input:
      - git clone https://github.com/LutherJolliff/angular_basic.git &&
        cd angular_basic && 
        npm install &&
        npm start
